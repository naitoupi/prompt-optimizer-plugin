# v0.13.0 — 先读你的上下文，再改写

这个版本让「✨ 优化」在动笔之前，**先读一遍你当前这个对话**。

## 🤔 之前的问题

插件只看得到输入框里那**一句话**。所以草稿里只要出现指代，改写就必然架空：

| 你写 | 之前的结果 |
|---|---|
| “把这个也改一下” | 模型不知道“这个”是什么，只能补一堆 `<…>` 占位符 |
| “继续” | 被判成“没有任务”，原样返回 |
| “按刚才说的那样加个焦点处理” | 不知道“刚才”指哪个项目、哪套代码 |

## ✨ 现在怎么做

点 ✨ 时，先取当前会话的上下文，再连同草稿一起交给模型：

| 读什么 | 说明 |
|---|---|
| 最近的对话正文 | 只取「你」和「AI」的**正文文本**；默认最近 12 条、合计 4000 字符、单条 900 字符 |
| 会话主题 | 会话标题 |
| 工作目录 | 例如 `D:\AndroidStudioProjects\H1` |
| Agent 预设 | standard / cordis 等 |

于是“把这个也改一下”会被改写成**指名道姓**的完整提示词；草稿本身没有任务、但上下文能确定意图时
（例如只有一句“继续”），也会把该做的事补成完整提示词 —— 这是本次行为上最明显的变化。

## 🧠 刻意不读什么（实测取舍）

拿一场真实会话数出来的：

| 消息块类型 | 条数 | 取不取 |
|---|---|---|
| `user:text`（你说的话） | **5** | ✅ |
| `assistant:text`（AI 正文） | 18 | ✅ |
| `user:tool-result`（工具结果） | **133** | ❌ |
| `assistant:tool-call`（工具调用） | 38 | ❌ |
| `assistant:reasoning`（思考过程） | 23 | ❌ |
| `system:text`（工作区指令） | 1 | ❌ |

工具结果条数是**你说的话**的 26 倍（133 vs 5），不筛掉就会被淹没。
另一个坑：**DSH 会把 `<system-reminder>`（技能清单等）当作 `user` 消息注入**，
实测该会话 `deriveMessages()` 的最后一条 user 正文正是技能清单 ——
不过滤就会把系统提醒当成“最近需求”。图片与附件同样不取。

## 🛡 边界与降级

- 上下文只用来**消解指代、补足背景**：不得整段照抄，不得据此新增你没提过的要求，草稿与上下文冲突时**以草稿为准**；
- 上下文**不是任务**：不会去回应它，也不会顺着它继续做事；
- **读不到就退回“只改草稿”**（总开关关闭 / 会话 id 缺失 / 服务不可用 / 会话不在内存 / 读取抛错 / 没有正文），
  载荷与 v0.12.5 **逐字一致**，不报错、不空转。

## 📊 用量显示在哪（v0.13.1 修正）

- 工具行只留短提示（`已优化并替换输入框 · 上下文 12 条`），6 秒淡出；
- **完整用量常驻在「📋 原文」参考区**：`🧠 已参考上下文：12 条历史（共 3874 字符，较早的 3 条未带入）`，
  一直显示到草稿被清空 / 发送 / 撤销为止。

v0.13.0 最初把长句塞进 300px 宽的工具行、3.2 秒就淡出 —— “还没看清就没了”，故改为常驻参考区。

## ⚙️ 新增设置

设置 → 提示词优化 → 「会话上下文」：开关（默认**开**）+ 长度上限（默认 **4000** 字符）。
关掉即完全退回旧行为。代价说明：因为要多发一段历史给模型，单次优化会**慢一点、费一点额度**。

## 🧪 测试

`tests/context.test.mjs` 新增 **46 项**：注入块过滤、只取正文块、预算与条数上限、超长截断、
中英标签、标题缺失、6 条降级路径，以及“**无上下文时载荷与旧版逐字一致**”的回归断言。
既有测试不变：`tests/tidy-layout.test.mjs` 11 项、`tests/builtin-prompt-detect.test.mjs` 27 项。

## 📦 升级方式

```powershell
dsh plugin --profile web remove prompt-optimizer-plugin
dsh plugin --profile web add github:naitoupi/prompt-optimizer-plugin
```

然后重启 `dsh web`。

**许可**：MIT
