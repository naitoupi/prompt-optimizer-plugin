# v0.12.4 — 别让"过期页面"把新版指令顶掉

这个版本修一个很隐蔽但后果严重的坑：**用户可能一直在用旧版优化指令，而插件和界面都不知道。**

## 🐛 问题是怎么发生的

`isBuiltinPrompt()` 只做**当前版本**的精确比对。于是：

1. 你发布了新版插件（内置指令变了），用户重启了 DSH；
2. 但用户的浏览器里，设置页**一直开着没刷新**，编辑器里还留着**上一版内置指令的文本**；
3. 用户点了「保存指令」（可能只是想确认一下）；
4. 旧版文本对 `isBuiltinPrompt()` 是陌生字符串 → **被当成"自定义指令"存下**。

结果：用户被静默钉死在旧指令上，**新版规则全部失效** —— 而他看到的界面还写着"当前使用：自定义版本"，根本不知道这是旧版默认值。

我在实测里复现了：把 v0.12.2 的内置文案（789 字符）提交上去，返回 `isCustom=true`，`state.prompt` 被写入 —— v0.12.3 的占位符边界规则对该用户完全不起作用。

## ✨ 两道防线

**第一道（Host）：按结构识别，而不是按版本比对**

新增 `looksLikeBuiltinPrompt()`，看三个结构锚点：

- 有独占一行的 `要求:` / `Rules:`
- 编号行 ≥ 11 条（工作步骤 3 + 要求 8）
- 含"提示词工程师" / "prompt engineer" 角色句

满足即视为内置指令 → 清掉覆盖、回落到**当前**内置版，并在响应里带 `replacedEarlierBuiltin`，让界面如实提示：

> 这是旧版内置指令：已自动改用当前内置版本（未存为自定义指令）

之所以不写死历史全文：内置文案以后还会改，结构锚点比版本清单耐用，也不用维护。

> 补充一句过程：我第一版设计是"在进程内存里累加历届内置指纹"。**那是错的** —— 进程一重启集合就是空的，而这个问题恰恰只在跨版本重启后出现，等于完全接不住。所以推倒重做成结构识别。

**第二道（客户端）：每次展开都重新拉取**

编辑器原先只在**首次展开**时拉取内容（`promptText !== null` 就跳过），所以不刷新页面就永远显示旧文本 —— 这正是坑的入口。现在每次展开都重新拉取。

同时保留**未保存的草稿**：只有在草稿等于基准文本时才覆盖，所以折叠再展开不会丢掉你正在编辑的内容（这个细节是我实现时发现自己引入的副作用，补上了）。

## 🧪 测试

新增 `tests/builtin-prompt-detect.test.mjs` —— **13 项**：

```powershell
node tests/builtin-prompt-detect.test.mjs
```

覆盖：当前/旧版内置识别、CRLF 与尾部换行变体、**真正的自定义指令不被误判**、只有角色句没有规则结构仍算自定义、内置+新增一条规则仍算内置。

另外对**真实服务**做了 15 项端到端断言，包括完整的"过期页面"场景：

| 场景 | 结果 |
|---|---|
| 提交旧版内置文案 | 不存为自定义，返回 `replacedEarlierBuiltin`，回落到当前版 |
| 提交真正的自定义指令 | 正常存为自定义，无该标记 |
| 「恢复默认指令」 | 返回当前内置版 |
| 提交当前内置版 | 视为默认，不带该标记 |
| 英文模板同场景 | 同样正确 |

测试过程中还抓到我自己写的正则 bug：`\d+\)\s*[\s\S]*?\n\s*(?:要求\|Rules)` 里的非贪婪 `*?` 让 `\d+` 退化成匹配单个数字，跨行匹配直接失效 —— 已改成"整行匹配 `要求:`"的简单判据。

## 📦 升级方式

```powershell
dsh plugin --profile web remove prompt-optimizer-plugin
dsh plugin --profile web add github:naitoupi/prompt-optimizer-plugin
```

然后重启 `dsh web`。

**如果你现在正开着设置页**：升级后请刷新一次页面，编辑器就会显示当前内置指令。即使忘了刷新、直接点了保存，v0.12.4 也会自动纠正，不会再把你钉在旧版上。

**许可**：MIT
