# v0.12.0 — 装了就该看得出来效果

这个版本解决一件事：**以前点了 ✨ 优化，输入框几乎没变化。**

同时把设置入口搬到更好找的位置，并让插件默认就处于最佳状态 —— 装完不用做任何设置。

## 🐛 为什么以前"点了和没点区别不大"

不是代码 bug，是内置优化指令的设计问题。旧指令里有 5 条「逃生条款」：

- 只在确有改进空间时改写，**已经清楚就逐字原样返回**
- **最小化改动**，避免无意义的措辞替换
- 改写前先理清"谁对谁做了什么"，**若原文已清晰正确就直接逐字返回**

结果模型面对一段大致通顺的草稿，最省事的合规做法就是原样返回；而唯一允许"扩写"的规则，又把扩写导向了**追加一句"请先向我确认需求"** —— 任务本身依然模糊。

修复前实测 7 条草稿：**2 条原样返回、1 条只删了一个"的"字、2 条只追加了一句追问。**

## ✨ 现在怎么做

**1. 指令改成正向任务定义**

> 读懂核心意图（锚点，不得改变）→ 诊断 **A)任务 B)输入 C)输出 D)约束 E)上下文** 缺哪些 → **只补缺的**

保留三条硬约束：不改语法角色与语义关系、不编造原文没有的要求、改写后不得比原文更短（防信息丢失）。

**2. 默认参数按实测调整**

| 参数 | 默认值 | 依据 |
|---|---|---|
| 思考强度 | `off` | `high` 只多花 8~15 秒，零质量收益 |
| 最大输出 | `1500` | 实测最长输出 ~174 tokens，8 倍余量 |
| 温度 | `0.3` | `0.1` 的语义就是"尽量照抄原文" |

**3. 统一批草稿的前后对比**

| | 修复前 | 修复后 |
|---|---|---|
| 原样返回 | 2 / 7 | **0 / 7** |
| 伪改动（只删一个字） | 1 / 7 | **0 / 7** |
| 单次耗时 | 3~30 秒 | **0.6~1.7 秒** |

**4. 不再有"看不出区别"的改动**

如果草稿确实已经清晰，插件会保持原文不变并提示「无需优化」；只差一两个标点时同样不改动，而是提示「本次改动极小」。两种情况都不会污染撤销栈 —— 真正有改进空间时输入框才会被替换。

## 🔀 设置入口搬家

从「设置 → 插件 → 提示词优化」标签页，搬到**设置左侧导航的一级条目**「提示词优化」（在「插件」下面，与「通用设置」「模型」并列）。少点两层，插件区也不再混着插件自己的设置页。

## 🆕 设置页新增说明卡

面板顶部写明：**推荐用法：什么都不用改** —— 附上推荐参数值，以及改坏了该点哪个「恢复默认」。

## 📦 升级方式

```powershell
dsh plugin --profile web remove prompt-optimizer-plugin
dsh plugin --profile web add github:naitoupi/prompt-optimizer-plugin
```

然后重启 `dsh web`。想彻底恢复推荐设置，删掉 `~/.dsh/prompt-optimizer-state.json` 再重启。

## 📝 其它

- 补回「无任务草稿原样返回」规则：修掉重写过程中引入的回归 —— `hello` 这类问候曾被改写成一句回应
- 规则补充「每项要求只出现一次」，修掉同义反复
- README 精简，去掉重复表述
- 内置优化指令仍是中英双语、可在设置页随时编辑并一键恢复默认

**完整改动**：`src/host.js`（指令与阈值）、`src/client.js`（设置条目、说明卡、提示文案）、`README.md`、`DEV.md`、`package.json`

**许可**：MIT
