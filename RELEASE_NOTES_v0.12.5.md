# v0.12.5 — 你保存什么就是什么

这个版本做的是**减法**：删掉 v0.12.4 引入的一整套"猜测机制"。

## 🙅 之前错在哪

v0.12.4 为了修一个坑（过期页面把旧版内置文案存成自定义），我在服务端加了一套判断"这段文本是不是旧版内置指令"的逻辑。两版都失败了：

| 尝试 | 结果 |
|---|---|
| 第一版：只比对当前内置版 | **太窄** —— 正是它要修的那个问题 |
| 第二版：结构判据（规则骨架 + 角色句） | **太松** —— 用户自己改过的指令也被判成"旧版内置"，**编辑被丢弃**并弹出"已自动改用当前内置版本"（实测 6/6 真实编辑全部误伤） |

第二版比它要修的问题**更糟**：原来的坑只是"可能用着旧指令"，而第二版是"直接丢掉用户刚保存的东西"。

## ✨ 现在的规则（只有一条）

> **当前内置文案 = 未自定义；其余一律按用户的自定义指令原样保存。**
> **插件不干预。** 不满意就点「恢复默认指令」。

就这么简单。用户点了保存，那就是用户的意思。

## 🔧 具体删掉了什么

- 4 段历届内置文案常量（中英 × 两版，约 6000 字符）
- `LEGACY_BUILTIN_PROMPTS` 列表与 `isBuiltinLikePrompt()`
- `replacedEarlierBuiltin` 响应字段与界面上那句"这是旧版内置指令…"提示

`host.js` 从 **724 行回到 681 行**。测试里有断言专门防止这些机制复活。

## 🛡 那原来的坑怎么办

**在源头解决，不在服务端猜。** 编辑器**每次展开都从 Host 重新拉取**当前内置文案，所以跨版本一直开着的设置页也不会把过期文本存回去 —— 它每次打开显示的就已经是最新版本。

（这一点 v0.12.4 就做对了，保留。）

## 🧪 测试

`tests/builtin-prompt-detect.test.mjs` 重写为 **23 项**：

- 当前内置版（中/英）→ 判为"未自定义"（标签显示「内置默认版」）
- **13 种用户文本 → 全部按自定义保存**，绝不被特殊对待：改一个字、加强某条规则、整条替换、追加规则、开头加行、中间插行、截断、旧版本、完全自定义、英文改动、仅空白差异、空串、纯问候
- 6 项断言：那套猜测机制的痕迹不得出现在 `host.js` / `client.js`

## 📦 升级方式

```powershell
dsh plugin --profile web remove prompt-optimizer-plugin
dsh plugin --profile web add github:naitoupi/prompt-optimizer-plugin
```

然后重启 `dsh web`。

**许可**：MIT
