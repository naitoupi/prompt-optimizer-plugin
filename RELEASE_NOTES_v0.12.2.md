# v0.12.2 — 排版收紧、稳定性与编码修复

这个版本修掉三个真实问题：**优化结果的排版因界面语言而异**、**重复优化结果不稳定**、**两处会让 DSH 启动失败或文字损坏的编码事故**。

## 🔀 优化结果不再"多出一堆空行"

**现象**：同一句草稿，界面语言是中文时输出单段，界面语言是英文时输出"任务/输入/输出/约束/上下文"逐行分段、且**每行之间都有一个空行**。

**原因**：插件按页面语言（`<html lang>`）选择指令模板，英文模板的输出风格偏分段加空行，中文模板偏单段。同一个人的同一句草稿，只因界面语言不同就得到两种排版 —— 看起来像格式异常。

**修法**：Host 侧新增排版整理 —— **删掉两个非空行之间的空行**。实测你报告的那句（`我想做一个 dsh 的插件，作用是可以设置 DSH 的背景`）：

| | 改前 | 改后 |
|---|---|---|
| 换行数（英文界面） | 12 | **6** |
| 分段标签 | `任务/输入/输出/约束/背景` | 全部保留 |
| 内容 | — | 逐行零丢失 |

**保护性保留**：代码围栏内部、列表项前后、段落之间的空行一律不动 —— 否则会把代码块压坏。可用 `tidyLayout: false` 关闭。

## 🎯 重复优化更稳定

对同一句提示词连点 5 次优化，以前会得到 5 个不同结果（长度 113~175 字，波动 55%）。已把默认温度从 `0.3` 调回 `0.1`。

**如实说明**：`0.1` 只是收窄了波动范围，**没有做到完全一致** —— 剩下的方差来自模型采样本身，不是温度能控制的。但你不用担心，因为：

- **对已优化过的草稿连点 5 次，5 次全部返回"无需优化"** —— 它是个稳定不动点，不会被越改越坏
- 8 次重复优化得到的变体，语义要素（否定词、数量、关键文件名）**100% 一致**，差异只是补充的详略
- 所有结果都**严格长于原文**，不会把好提示词改短

## 🧪 新增回归测试

`tests/tidy-layout.test.mjs` —— 11 项，覆盖代码块保护、列表保护、段落空行、CRLF、纯空白行、以及"非空行零丢失"不变量。

```powershell
node tests/tidy-layout.test.mjs
```

测试直接从 `src/host.js` 抽取函数体执行，测的是真实实现而不是副本。**它当场抓出一个真 bug**：只含空格的行没被当成空行处理。

## 🔧 两处编码事故（都已根治）

1. **BOM 导致 `dsh web` 启动失败**：版本号被用 Windows PowerShell 5.1 的 `Set-Content -Encoding UTF8` 写入，该参数会悄悄加上 UTF-8 BOM，而 DSH 的 profile 加载器对每个 bundle 的 `package.json` 直接 `JSON.parse`、不去除 BOM，于是启动即报 `SyntaxError: Unexpected token ''`。现已新增 `.editorconfig`（`charset = utf-8`）并在 `DEV.md` 写明复现方式与三种正确写法。

2. **`package.json` 里的 ✨ 被损坏**：同一根因（PowerShell 5.1 把 UTF-8 字节按 GBK 解码）。已按字节精确重建，并对全部 13 个文本文件做了编码体检：**无 BOM、无不可解码字节、无乱码**。

## 📦 升级方式

```powershell
dsh plugin --profile web remove prompt-optimizer-plugin
dsh plugin --profile web add github:naitoupi/prompt-optimizer-plugin
```

然后重启 `dsh web`。

## 📝 版本脉络

- **v0.12.0**：设置入口迁到左侧导航 + 修复"点了优化和没点区别不大"
- **v0.12.1**：稳定性（温度）+ BOM 启动修复
- **v0.12.2**：排版收紧 + 回归测试 + 编码体检

**许可**：MIT
