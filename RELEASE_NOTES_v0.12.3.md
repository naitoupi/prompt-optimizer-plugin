# v0.12.3 — 优化指令收紧了"占位符"

一个小而值得的修复：**短草稿不再被塞进一堆"可选项追问"。**

## 🐛 之前是什么样

内置指令的规则 6 只写了"无法从草稿推断的取值用尖括号标出" —— **既没有标准，也没有边界**。于是短草稿会被填入大量**可选项**，而不是真正缺失的关键信息：

| 草稿 | 输出 | 膨胀 |
|---|---|---|
| 帮我写个二分查找（8 字） | 176 字，**7 个占位符** | 22 倍 |
| 帮我写周报（5 字） | 116 字，3 个占位符 | 23 倍 |

其中约一半是这类枚举：

```
<是否要求递归或迭代实现>、<代码是否需要包含注释>、
<是否允许重复元素，以及若允许应返回哪一个下标>
```

这些不是"用户没说清楚"，而是"所有可能情况的清单" —— 用户看到会觉得这是一张表格，不是一条提示词。

## ✨ 现在怎么改

规则 6 补上两条边界（中英双语）：

1. **每个占位符只标一件具体的事**，不得把无关的要求拼在一起；
2. **不要为了"完整"而逐条枚举各种可能情况**，只标真正影响结果的关键信息。

## 📊 实测对比（同一批 5 条草稿）

| 指标 | 改前 | 改后 |
|---|---|---|
| 占位符总数 | 22 | **15** |
| 输出总长度 | 710 字 | **605 字** |
| 二分查找那条 | 7 个占位符 | **2 个** |
| 结构完整度 | 输入/输出/约束 | 完整保留 |
| 语义（否定词、数量、代码块） | 保持 | 保持 |

"帮我写个二分查找"现在的输出：

```
请帮我写一个二分查找的实现。
输入：<待查找的有序数组、目标值，以及数组的排序方式（升序或降序）>。
输出：给出二分查找的完整代码，并说明返回值在找到目标值和未找到目标值两种情况下的含义。
约束：使用 <编程语言> 编写；代码需包含必要的边界处理；说明时间复杂度和空间复杂度。
```

三个占位符都值得用户动手填，之前的"是否需要注释、是否需要说明复杂度"这类追问消失了。

## 🙅 顺便说一个被否决的改法

我先试了更"直接"的方案：**给占位符加"最多三个"的数量上限**。

指标非常漂亮 —— 占位符 22→6、长度减半。但**质量塌方**：

- 模型把不相关的要素塞进同一个括号：「数组及目标值，**或**语言及版本」（用户没法填）
- 或塌成废话：「输出形式与验收标准：\<期望的输出形式与验收标准\>」

**硬性数量上限会逼模型把不同要素合并成空泛占位符**，反而不如原来。这条已作为"勿重复尝试"写进 `DEV.md`。

## 🧪 顺带补了改指令的验证方法

`DEV.md` 新增一节，写明改指令前该怎么验证：

1. 用 `GET /prompt?lang=zh` 取出**线上生效**的指令；
2. 用 `POST /prompt` 安装候选，跑同一批固定草稿（短/中/长、含代码块、纯问候各一），记录占位符数、输出长度、分段标签数；
3. 用 `POST /prompt {reset:true}` 还原，并确认 `isCustom=false`。

以后改指令照着走，避免再出现"以为更好、其实更差"。

## 📦 升级方式

```powershell
dsh plugin --profile web remove prompt-optimizer-plugin
dsh plugin --profile web add github:naitoupi/prompt-optimizer-plugin
```

然后重启 `dsh web`。也可以直接在 **设置 → 提示词优化 → 优化指令** 里点「恢复默认指令」拿到新版内置指令。

**许可**：MIT
